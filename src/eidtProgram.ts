import { spawnSync } from "child_process";

const windowsExe = ["code", "atom", "vim", "vi", "notepad", "start"];
const unixExe = ["code", "atom", "vim", "vi", "mate", "open"];



export function editOSSFile(urL: string) {

}

export function editFile(file: string) {
  
}
